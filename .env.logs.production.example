# 🌐 生產環境日誌配置範例
# 複製此檔案為 .env.logs.production 並修改相關設定

# 生產環境日誌服務器配置
# 替換為你的生產服務器域名或 IP
EXPO_PUBLIC_REMOTE_LOG_URL=https://logs.yourcompany.com:5001/api/logs

# 或者指定生產服務器 IP
# EXPO_PUBLIC_DEV_LOG_IP=your-production-server-ip

# 安全配置
# 在生產環境中，建議禁用某些日誌級別以減少資料傳輸
EXPO_PUBLIC_LOG_LEVEL=warn,error

# 如果需要完全禁用遠程日誌（僅使用本地日誌）
# EXPO_PUBLIC_DISABLE_REMOTE_LOGS=true

# API 金鑰（如果日誌服務器需要認證）
# EXPO_PUBLIC_LOG_API_KEY=your-secure-api-key

# 日誌服務配置
EXPO_PUBLIC_LOG_BATCH_SIZE=50
EXPO_PUBLIC_LOG_FLUSH_INTERVAL=30000

# 服務識別
EXPO_PUBLIC_SERVICE_NAME=localite-app-production
EXPO_PUBLIC_ENVIRONMENT=production

# 使用方式：
# 1. 複製此檔案： cp .env.logs.production.example .env.logs.production
# 2. 修改生產服務器設定
# 3. 切換到生產環境： cp .env.logs.production .env
# 4. 重新建置應用： eas build --platform all --profile production
